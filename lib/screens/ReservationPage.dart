import 'package:flutter/material.dart';

class ReservationPage extends StatelessWidget {
  static const id = 'reservation_page';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
